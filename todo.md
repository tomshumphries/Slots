Fix payout info + rule sheet, using values stored in game config (no hard codes)

Add a tracker for current session (with a reset button) - tracks count, num bonus's, a graph of balance over time etc. 